// Auto-generated MDN test fragment (Node.js reference runner)
// Category: expressions, Fragment: 72
// Source: test_expressions_part*.js
// Run: node test_expressions_frag_72.node.js

function testExpressions_frag_72() {
    try {

        let bar = 5;

        bar -= 2; // 3
        } catch (e) {
        console.error(`[testExpressions_frag_72] error: ${e.message}`);
    }
}

// Self-test when run directly
if (require.main === module) {
    testExpressions_frag_72();
}

module.exports = { testExpressions_frag_72 };
