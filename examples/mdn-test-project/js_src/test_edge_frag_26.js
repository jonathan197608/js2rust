// Auto-generated edge-case test fragment (Zig transpile target)
// Category: edge, Fragment: 26
// Targeting recurring bug patterns from R17-R30 deep audits

export function testEdge_frag_26() {

    const name = "World";
    const count = 3;
    const items = ["a", "b", "c"];
    console.log(`Hello ${name}!`);
    console.log(`Count: ${count}, Items: ${items.join("-")}`);
    console.log(`Math: ${1 + 2 * 3}`);
    console.log(`Nested: ${`inner ${1 + 1}`}`);
    console.log(`Multi
    line
    string`);

}
